# Docker Compose (opcional)
# Se você criar um `.env` na raiz, o Docker Compose usa essas variáveis para
# parametrizar o `docker-compose.yml` (se você optar por usar ${...} nele).
POSTGRES_DB=desafio
POSTGRES_USER=app
POSTGRES_PASSWORD=app

# API (valores padrão do compose atual)
PORT=3000
DB_HOST=db
DB_PORT=5432
DB_NAME=desafio
DB_USER=app
DB_PASS=app
STORAGE_ROOT=/app/storage
MAX_UPLOAD_MB=10
CORS_ORIGIN=http://localhost:5173


# Frontend (React + Tailwind) via Docker

## Subir o projeto

Na pasta `frontend/`:

```bash
docker compose up --build
```

Depois acesse `http://localhost:5173`.

## Parar

```bash
docker compose down
```

